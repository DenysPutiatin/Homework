def draw_tree(num_triangles: int):
    # Змінна, яка зберігає поточну кількість зірочок для рядка
    current = 1
    # Список для збереження кількості зірочок у кожному рядку
    star_counts = []
    
    # Для кожного трикутника (рівня)
    for level in range(num_triangles):
        height = level + 3  # кожен наступний трикутник має на 1 рядок більше (мінімум 3 рядки)
        # Для кожного рядка цього трикутника
        for _ in range(height):
            star_counts.append(current)
            current += 2  # кожен наступний рядок має на 2 зірочки більше
        # При переході до наступного трикутника зменшуємо поточну кількість зірочок,
        # щоб наступний рівень починався трохи "ліворуч" (типовий ефект ялинкової форми)
        current -= 2 * ((level // 2) + 1)
    
    # Максимальна ширина – це кількість зірочок у останньому рядку
    max_width = star_counts[-1]
    
    # Будуємо рядки ялинки використовуючи list comprehension (iterator)
    lines = [(" " * ((max_width - stars) // 2)) + ("*" * stars) for stars in star_counts]
    
    # Вивід ялинки в консолі (один print)
    print("\n".join(lines))


if __name__ == '__main__':
    try:
        # Єдиний параметр для конфігурації – кількість трикутників
        num = int(input("Введіть кількість трикутників: "))
        draw_tree(num)
    except ValueError:
        print("Будь ласка, введіть ціле число!")
